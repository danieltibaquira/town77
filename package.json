{
  "name": "town77",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "@rollup/rollup-darwin-arm64": "4.61.1",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
